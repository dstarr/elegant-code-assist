import * as vscode from 'vscode';
import { Command } from './Command';

export class ShowModelsCommand implements Command {
    
    public readonly name: string  = 'ec-assist.showModels';

    public execute(): void {


    }
}